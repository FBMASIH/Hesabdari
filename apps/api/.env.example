DATABASE_URL=postgresql://hesabdari:hesabdari@localhost:5432/hesabdari?schema=public
VALKEY_HOST=localhost
VALKEY_PORT=6379
RABBITMQ_URL=amqp://hesabdari:hesabdari@localhost:5672
API_PORT=4000
API_HOST=0.0.0.0
NODE_ENV=development
JWT_SECRET=dev-secret-change-in-production
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# CORS: comma-separated list of allowed origins.
# In development mode, all origins are allowed regardless of this setting.
# In production, list your actual domain(s):
#   API_CORS_ORIGINS=https://app.example.com,https://admin.example.com
API_CORS_ORIGINS=http://localhost:3000
